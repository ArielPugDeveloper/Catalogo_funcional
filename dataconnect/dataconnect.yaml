specVersion: "v1"
serviceId: "catalogo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "catalogo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
